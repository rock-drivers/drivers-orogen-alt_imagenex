# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(alt_imagenexTaskLib)
ADD_LIBRARY(${ALT_IMAGENEX_TASKLIB_NAME} SHARED 
    ${ALT_IMAGENEX_TASKLIB_SOURCES})

add_dependencies(${ALT_IMAGENEX_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${ALT_IMAGENEX_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${ALT_IMAGENEX_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${ALT_IMAGENEX_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${ALT_IMAGENEX_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${ALT_IMAGENEX_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${ALT_IMAGENEX_TASKLIB_HEADERS}
    DESTINATION include/orocos/alt_imagenex)

